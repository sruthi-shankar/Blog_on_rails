<div class="row">
    <div class="=col-2">
    </div>

    <div class="col-10">
    <% @posts.each do |post| %>
        <div>
            <h3><%= link_to post.title, posts_path('post') %></h3>
           
        </div>
    <% end %>
    </div>